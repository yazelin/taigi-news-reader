{
  "name": "taigi-news-reader-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that reads Traditional Chinese news aloud in Taiwanese.",
  "scripts": {
    "clean": "node scripts/clean.js",
    "build": "node scripts/build.js",
    "test": "node --test",
    "lint": "eslint .",
    "check": "npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "esbuild": "^0.25.6",
    "eslint": "^9.31.0",
    "globals": "^16.3.0",
    "jsdom": "^26.1.0"
  }
}
