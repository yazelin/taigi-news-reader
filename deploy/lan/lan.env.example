# Compose-only settings. Copy to lan.env on the backend host.
TAIGI_BACKEND_ENV_FILE=./backend.env

# Set to 1 for the non-commercial local facebook/mms-tts-nan reference model.
# Set to 0 only when TAIGI_TTS_PROVIDER=remote is configured in backend.env.
TAIGI_INSTALL_LOCAL_MMS=1
